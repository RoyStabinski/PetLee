# Screenshots

Evidence for `../e2e-scenarios.md`. One per scenario, plus the two the task calls out.

| File | Shows |
|---|---|
| `scenario-1-guest-gallery.png` | The gallery as a guest, with the contact block replaced by a login prompt |
| `scenario-2-listing-posted.png` | A newly posted listing at the top of the gallery, with its thumbnail |
| `scenario-3-contact-details.png` | The same listing seen by a logged-in member, contact details visible |
| `scenario-4-admin-panel.png` | The admin panel, both tables |
| `scenario-4-delete-confirmation.png` | The confirmation naming the pet |
| `category-in-use-refused.png` | A category that still holds listings, with its Delete disabled and the reason |

PNG, full window, at 1440 px unless the step is about a narrow screen.
